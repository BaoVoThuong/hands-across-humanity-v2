---
import { Picture } from 'astro:assets';
import {
  partnerCommunity,
  educationRelief,
  communityDistributionWide,
  field02,
  partnerShowcase,
} from '../../assets/images/eps';

interface Props {
  title?: string;
  lead?: string;
  donateUrl?: string;
  exploreUrl?: string;
}

const {
  title = "A community donation network.",
  lead = "Hands Across Humanity brings together donated goods, volunteers, and local partners to share practical support with children, families, veterans, and neighbors.",
  donateUrl = "/what-we-do/",
  exploreUrl = "/events/#upcoming"
} = Astro.props;

const slides = [
  {
    image: partnerCommunity,
    alt: "Community partners and families gathered with prepared support packages.",
    lead: "Community partnership",
    title: "Support arrives through the people a community already trusts."
  },
  {
    image: educationRelief,
    alt: "Children holding prepared school and relief kits with community volunteers.",
    lead: "School & family support",
    title: "Putting education essentials into the hands of children and families."
  },
  {
    image: partnerShowcase,
    alt: "Volunteers and partners gathered behind a table of essential supplies.",
    lead: "Community-powered giving",
    title: "Turning generosity into practical support for communities under pressure."
  },
  {
    image: field02,
    alt: "Volunteers and community members gathered with prepared backpacks for distribution.",
    lead: "People-powered support",
    title: "A community network turns shared resources into practical care."
  },
  {
    image: communityDistributionWide,
    alt: "Volunteers and community members coordinating support at a distribution event.",
    lead: "Community distribution",
    title: "Sharing support through relationships built on dignity and trust."
  }
];
---

<section class="hero-v2" id="hero">
  <div class="container">
    <div class="hero-v2__grid">
      <div class="hero-v2__copy">
        <h1 class="hero-v2__title reveal" style="--i:1">{title}</h1>
        <p class="hero-v2__lead reveal" style="--i:2">{lead}</p>

        <div class="btn-row reveal" style="--i:3">
          <a class="btn btn--primary btn--lg" href={donateUrl}>
            See our work<span class="btn__arrow" aria-hidden="true">→</span>
          </a>
          <a class="btn btn--ghost btn--lg" href={exploreUrl}>
            Upcoming events
          </a>
        </div>

        <div class="hero-v2__proof reveal" style="--i:4" aria-label="How support moves">
          <span class="hero-v2__proof-item">Donated goods</span>
          <span class="hero-v2__proof-item">Volunteer-powered</span>
          <span class="hero-v2__proof-item">Local partners</span>
        </div>
      </div>

      <div class="hero-v2__media-col reveal" style="--i:3">
        <div class="hero-carousel" id="hero-carousel" aria-label="Field dispatch photo showcase" role="region">
          {/* Progress bar indicator */}
          <div class="hero-carousel__progress-track" aria-hidden="true">
            <div class="hero-carousel__progress-bar" id="hero-carousel-progress"></div>
          </div>

          {/* Slides stack */}
          <div class="hero-carousel__slides">
            {slides.map((slide, index) => (
              <div
                class={`hero-carousel__slide ${index === 0 ? 'is-active' : ''}`}
                data-slide-index={index}
                aria-hidden={index === 0 ? "false" : "true"}
              >
                <Picture
                  src={slide.image}
                  widths={[600, 900, 1200]}
                  sizes="(min-width: 960px) 55vw, 100vw"
                  formats={['avif', 'webp']}
                  fallbackFormat="jpeg"
                  quality={85}
                  alt={slide.alt}
                  loading="eager"
                  fetchpriority={index === 0 ? "high" : "auto"}
                />
              </div>
            ))}
          </div>

          {/* Navigation Arrows (Visible on hover) */}
          <button class="hero-carousel__arrow hero-carousel__arrow--prev" id="hero-carousel-prev" aria-label="View previous field photo slide" type="button">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <polyline points="15 18 9 12 15 6"></polyline>
            </svg>
          </button>
          <button class="hero-carousel__arrow hero-carousel__arrow--next" id="hero-carousel-next" aria-label="View next field photo slide" type="button">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <polyline points="9 18 15 12 9 6"></polyline>
            </svg>
          </button>

          {/* Indicator dots */}
          <div class="hero-carousel__indicators">
            {slides.map((_, index) => (
              <button
                class={`hero-carousel__dot ${index === 0 ? 'is-active' : ''}`}
                data-goto-slide={index}
                aria-label={`Go to slide ${index + 1}`}
                type="button"
              ></button>
            ))}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  const DURATION = 5000;
  const carousel = document.getElementById('hero-carousel');
  if (carousel) {
    const slides = carousel.querySelectorAll('.hero-carousel__slide');
    const progressBar = document.getElementById('hero-carousel-progress');
    const prevBtn = document.getElementById('hero-carousel-prev');
    const nextBtn = document.getElementById('hero-carousel-next');
    const dots = carousel.querySelectorAll('.hero-carousel__dot');

    let currentIndex = 0;
    let timer: ReturnType<typeof setInterval> | null = null;

    function startProgressBar() {
      if (!progressBar) return;
      progressBar.style.transition = 'none';
      progressBar.style.width = '0%';
      // Trigger reflow to restart CSS transition
      void progressBar.offsetWidth;
      progressBar.style.transition = `width ${DURATION}ms linear`;
      progressBar.style.width = '100%';
    }

    function goToSlide(index: number) {
      if (index === currentIndex) return;
      slides[currentIndex]?.classList.remove('is-active');
      slides[currentIndex]?.setAttribute('aria-hidden', 'true');
      dots[currentIndex]?.classList.remove('is-active');

      currentIndex = (index + slides.length) % slides.length;

      slides[currentIndex]?.classList.add('is-active');
      slides[currentIndex]?.setAttribute('aria-hidden', 'false');
      dots[currentIndex]?.classList.add('is-active');

      restartTimer();
    }

    function nextSlide() {
      goToSlide(currentIndex + 1);
    }

    function prevSlide() {
      goToSlide(currentIndex - 1);
    }

    function startTimer() {
      stopTimer();
      startProgressBar();
      timer = setInterval(nextSlide, DURATION);
    }

    function stopTimer() {
      if (timer) {
        clearInterval(timer);
        timer = null;
      }
    }

    function restartTimer() {
      stopTimer();
      startTimer();
    }

    prevBtn?.addEventListener('click', (e) => {
      e.preventDefault();
      prevSlide();
    });

    nextBtn?.addEventListener('click', (e) => {
      e.preventDefault();
      nextSlide();
    });

    dots.forEach((dot, idx) => {
      dot.addEventListener('click', (e) => {
        e.preventDefault();
        goToSlide(idx);
      });
    });

    carousel.addEventListener('keydown', (e) => {
      if (e.key === 'ArrowLeft') {
        e.preventDefault();
        prevSlide();
      } else if (e.key === 'ArrowRight') {
        e.preventDefault();
        nextSlide();
      }
    });

    // Touch swipe gesture handling for mobile devices
    let touchStartX = 0;
    let touchEndX = 0;

    carousel.addEventListener('touchstart', (e: TouchEvent) => {
      touchStartX = e.changedTouches[0].clientX;
    }, { passive: true });

    carousel.addEventListener('touchend', (e: TouchEvent) => {
      touchEndX = e.changedTouches[0].clientX;
      const swipeDistance = touchEndX - touchStartX;
      if (Math.abs(swipeDistance) > 40) {
        if (swipeDistance < 0) {
          nextSlide();
        } else {
          prevSlide();
        }
      }
    }, { passive: true });

    startTimer();
  }
</script>
